{
  "name": "uxportfolio",
  "private": true,
  "start": "react-scripts --openssl-legacy-provider start",
  "build": "react-scripts --openssl-legacy-provider build",
  "scripts": {
   "serve": "echo \"This is executed 1\" && set NODE_OPTIONS=--openssl-legacy-provider &&  vue-cli-service serve",
   "build": "echo \"This is executed 1\" && set NODE_OPTIONS=--openssl-legacy-provider &&  vue-cli-service build"
   },
  "dependencies": {
    "@rails/actioncable": "^6.0.0-alpha",
    "@rails/activestorage": "^6.0.0-alpha",
    "@rails/ujs": "^6.0.0-alpha",
    "@rails/webpacker": "^5.4.0",
    "bootstrap": "4.3.1",
    "jquery": "^3.4.1",
    "popper.js": "^1.15.0",
    "turbolinks": "^5.2.0"
  },
  "version": "0.1.0",
  "devDependencies": {
    "webpack-dev-server": "^5.0.4"
  },
  "engines": {
     "node": "16.18.1"
   }
}
